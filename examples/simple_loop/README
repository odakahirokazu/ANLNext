This is an simple example of ANL application. This does a simple loop.

How to Install:
mkdir build
cd build
cmake ../source -DCMAKE_INSTALL_PREFIX=/path/to/install
make
make install

By default, the install destination is your home directory.

How to Run:
cd run
./run_anl_example.rb
